
  # Tuxedo Driver Mobile App UI (Copy)

  This is a code bundle for Tuxedo Driver Mobile App UI (Copy). The original project is available at https://www.figma.com/design/zP6QATIFfpVaa0mzF15Euc/Tuxedo-Driver-Mobile-App-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  